/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    // Incluimos todos los archivos (html, js) dentro de la carpeta src
    ".src/**/*.{html,js}'",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

